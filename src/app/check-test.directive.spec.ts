import { CheckTestDirective } from './check-test.directive';

describe('CheckTestDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckTestDirective();
    expect(directive).toBeTruthy();
  });
});
